# 课设项目策划书 - 团队文档仓库 (README)

本项目仓库是我们《高速公路实时车流监控与套牌车预警平台》项目的 **"唯一事实来源 (Single Source of Truth)"**。

所有关键决策、交付物、API 规范和服务器状态都将在此同步。**请所有组员（B 和 C）在开始工作前，务必先阅读本文档。**

## 1. 🚀 组员快速上手 (必读)

| **角色**          | **负责人**  | **核心职责 (一句话)**                                        | **你的“圣经” (参考文档)**              |
| ----------------- | ----------- | ------------------------------------------------------------ | -------------------------------------- |
| **前端 & 管道**   | **同学 A ** | 负责 **Kafka 数据流** (`traffic-raw`) 生产，以及 **Vue/React 前端** 开发。 | `杨研博-数据管道...`, `刘冰峰-前端...` |
| **存储架构 (主)** | **同学 B**  | 负责在 `db-app` 搭建 **MySQL 双节点 + MyCat 分片**，并编写 Flink 将数据入库。 | `朱少行-后端...` (MyCat部分)           |
| **算法预警 (辅)** | **同学 C**  | 负责编写 **Flink 实时报警** (存入 Redis) 和训练 **Spark 预测模型**。 | `杨再润-组长...` (Flink/模型部分)      |
| **后端 API**      | **同学 D**  | 负责开发 **Flask 统一接口**，连接 MyCat 和 Redis，为前端提供标准 API。 | `API.md`, `朱少行-后端...` (Flask部分) |

---

## 2. 📡 当前状态：数据管道已打通 (A组交付)

**(B、C 组必读)**

`pipeline-algo` 服务器 上的数据管道**已于 11-18 联调成功**，并转入 7x24h 后台运行。

**你们（B、C 组）的 Flink 作业 现在就可以开始连接：**

* **Kafka Broker 地址**: `localhost:9092`
* **消费主题 (Topic)**: `traffic-raw`

**重要提示**：
1.  **必须** 使用 `localhost:9092`。
2.  **严禁** 使用公网 IP。我们在联调中已确认，使用公网 IP 会导致 `LEADER_NOT_AVAILABLE` 致命错误。

所有服务器的详细配置（IP、端口、服务状态、运维日志）已记录在 **`团队虚拟机报告.md`** 中，请 B、C 组在开发前仔细阅读。

---

## 3. ⚠️ Git 协作指南 (新手必看)

我们的项目依赖这份文档，**如果你修改了本地文件（例如 `API.md`），你必须立刻同步到 Git 仓库**，否则其他人无法工作。

请严格遵守以下 "五步工作流"：

### 五步工作流 (每次修改后都必须执行)

#### 第 1 步：(开始修改前) 同步最新版本
**永远**在修改前，先拉取服务器的最新版本，防止冲突。
```bash
git pull origin main
```



#### 第 2 步：(本地) 修改你的文件

例如，你（同学B）修改了 `API.md` 并添加了新的 API。

#### 第 3 步：(本地) 添加所有改动

```bash
git add .
```



#### 第 4 步：(本地) 提交改动 (写清楚你干了什么)



```Bash
git commit -m "B组：更新了 /api/query 接口的分页参数"
```

*(请**务必**写清楚 `-m` 消息，例如 "A组：更新了VM报告" 或 "C组：更新了算法API定义")*



#### 第 5 步：(本地) 推送到服务器



```Bash
git push origin main
```

------



## 4. 🗂️ 文件目录结构说明



- `/` (根目录): 存放**高频变动**的状态报告 (如 `团队虚拟机报告.md`) 和**最终交付物** (如 `kkmc_dimension.csv`)。
  - **`课设项目策划书.md`**: **(核心)** 包含所有人的详细开发步骤、代码逻辑和服务器配置
  - `API.md` (或 `项目技术接口规范...`): 前后端对接的唯一契约。
  - `团队虚拟机报告.md`: 服务器 IP、端口、账号密码信息。
- `/需要喂给大模型的`: 存放**永不改动**的原始需求文档和项目规范 (我们的“知识库”)。
- `/需要喂给大模型的/往年学长方案`: 存放我们 4 个人的核心参考“圣经”。
- `/提示词`: (A组使用) AI 助手的规划日志，用于项目复盘。