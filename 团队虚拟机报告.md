### 1. 团队文档 (用于同步给 B、C 同学)



**主题：** `pipeline-algo` 服务器状态更新 - (同学A)

**日期：** 2025-11-18

概要：

我已完成 pipeline-algo 服务器 的核心环境搭建，Zookeeper 和 Kafka 服务均已启动并验证通过。

**详细状态：**

1. **操作系统：** Ubuntu 22.04
2. **核心依赖：** OpenJDK 1.8 (已安装)
3. **Zookeeper (v3.5.7)：**
   - 状态：**运行中** (Standalone 模式)
   - 端口：`2181`
4. **Kafka (v2.4.1)：**
   - 状态：**运行中**
   - Zookeeper 依赖：已连接 `localhost:2181`
   - **关键配置 (内存)：** 为节约 4GiB 服务器资源，已将 Kafka 堆内存限制在 1G (`-Xmx1G -Xms1G`)。
   - **关键配置 (网络)：** 已配置 `listeners=PLAINTEXT://0.0.0.0:9092` 和 `advertised.listeners=PLAINTEXT://<你的公网IP>:9092`。

**下游须知 (同学 B 和 C)：**

- 你们（Flink 消费者、算法 API）连接 Kafka Broker 的 **唯一地址** 是：`PLAINTEXT://<你的公网IP>:9092`。
- 后续我将配置 Flume 向 Kafka 的 `traffic-raw` 主题 写入数据。